var express = require('express');var router = express.Router();var con=require('../../modules/index/mysql');var ejs=require('ejs');//请求首页列表数据router.get('/', function(req, res, next) {    con.query('select * from context',function(err,rul,fiels){        if (err)throw err;        con.query('select * from hbarner ',function(err,rull,fiels){            if (err)throw err;             res.render('index', { data : rul , data1 :  rull });        });    });});module.exports = router;