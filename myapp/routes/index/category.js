var express = require('express');var router = express.Router();var con=require('../../modules/index/mysql');var ejs=require('ejs');// var ejs=require('ejs');/* GET home page. */router.get('/', function(req, res, next) {    con.query('select * from hcategory',function(err,rul,fiels){        if(err)throw err;        con.query('select * from context',function(err,rull,next){            if(err)throw err;            //数据处理，以及网页请求            res.render('./index/category', { data : rul , data1 : rull });        });    });});//详情加载router.get('/:id',function(req,res,next){    var id=req.params.id;    con.query('select * from context',function(err,rul,fiels){        if(err)throw err;        res.render('./index/show',{ data : rul });    });});module.exports = router;