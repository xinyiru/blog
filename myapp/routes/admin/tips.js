var express = require('express');var router = express.Router();var ejs=require('ejs');/* GET home page. */router.get('/:id', function(req, res, next) {    var codes=req.params.id;    console.dir(codes);    if(codes!=1){        //数据处理，以及网页请求        res.render('./admin/tips', { title: '失败',urls : '/hlogin' });    }else {        //数据处理，以及网页请求        res.render('./admin/tips', { title: '成功',urls : '/hindex' });    }});module.exports = router;